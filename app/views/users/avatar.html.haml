-title 'Редактровать аватар'
-content_for(:head_css) do
  =stylesheet_link_tag "jquery.Jcrop.min.css.scss"
-content_for(:head_js) do
  =javascript_include_tag "jquery.Jcrop.min"
:javascript
  $(function() {
    $('#avatarcrop').Jcrop({
      onChange: update_crop,
      onSelect: update_crop,
      setSelect: [50,50,450,450],
      aspectRatio: 1
    });
  });
  
  function update_crop(coords) {
    
    var ratio = "#{@user.avatar_geometry(:original).width / @user.avatar_geometry(:large).width}";
    $("#crop_x").val(Math.round(coords.x * ratio));
    $("#crop_y").val(Math.round(coords.y * ratio));
    $("#crop_w").val(Math.round(coords.w * ratio));
    $("#crop_h").val(Math.round(coords.h * ratio));
  }

.row
  .col-md-12
    .col-md-6
      =image_tag @user.avatar.url(:large), id: 'avatarcrop'
      =# f.file_field :avatar
    .col-md-6

      =form_for @user do |f|
        -for attribute in [:crop_x, :crop_y, :crop_w, :crop_h]
          =f.hidden_field attribute, id: attribute
        = f.submit "Обрезать",  {:class => "input btn btn-primary"}